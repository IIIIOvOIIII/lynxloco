# CHG260906006 final evidence

User requested8-way concurrency with180-second window and HTTP timeout, with no automatic rollback regardless of acceptance result. Deployment and one fixed30-minute live operating acceptance passed; candidate remains deployed. No extra synthetic requests in this cycle.

Live traces start and finish within20:30:58.380-21:00:58.380+08. Gateway aggregates use audit completion timestamps and therefore a different cohort. Completed successful cycles over120seconds and HTTP stages over120seconds are counted separately. Sampled memory maxima are not continuous high-water marks. Health/PID checks are point-in-time observations, supplemented by final native verification.

Queries export selected metadata/counts only; no camera images, prompts, descriptions, replies or credentials. CHG260906006 is now closed; future production execution of queries requires a new active CO/PAM.
