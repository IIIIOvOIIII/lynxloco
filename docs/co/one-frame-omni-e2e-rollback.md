# Rollback for read-only Miloco one-frame Omni E2E verification

No server-side rollback is expected because the approved activity is inspection-only and does not permit production mutation.

If any planned command would edit files, restart services, change settings, delete state, rotate credentials, alter firewall/listener behavior, or modify database content outside unavoidable existing application inference accounting, stop immediately and do not execute that command under this read-only CO.

If temporary frame data is created for the single-frame test, delete only that task-specific temporary directory before ending the session.

